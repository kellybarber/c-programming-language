#include <stdio.h>

int main(void) 
{
    int number = 0, nth = 0;

    printf("Input number to clear nth bit of: ");
    scanf("%d", &number);
    printf("Input n of nth bit: ");
    scanf("%d", &nth);

    return 0;
}